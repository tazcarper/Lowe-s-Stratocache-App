'use strict';

/**
 * @ngdoc function
 * @name stratocacheApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the stratocacheApp
 */
angular.module('stratocacheApp')
  .controller('MainCtrl', ['$scope', function ($scope) {
    $scope.bodyClass = 'main';
  }]);
